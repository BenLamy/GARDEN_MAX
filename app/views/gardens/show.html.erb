<h1><%= @garden.name %></h1>



<%= link_to 'Edit Garden', edit_garden_path(@garden), class: 'btn btn-primary' %>
<%= link_to 'Back to Gardens', gardens_path, class: 'btn btn-secondary' %>
<%= button_to 'Delete Garden', garden_path(@garden), method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } }, class: 'btn btn-danger' %>
